<nav class="navbar navbar-expand-lg navbar-dark navHome">
    <div id="navBrand">
        <a class="navbar-brand" href="{{path('home')}}"><img id="logoMenu" src="/images/menu_logo.png" alt=""></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="https://bookeo.com/daedalusescapegameblois/buyvoucher" class="btn btnOffre">Pour offrir !</a>
            </li>
            <li class="nav-item active acceuil">
                <a class="nav-link" href="{{path('home')}}">Accueil <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Les salles
                </a>
                <div class="dropdown-menu dropM" aria-labelledby="navbarDropdown">
                    {% for room in rooms %}
                        <a class="dropdown-item" href="{{path('room', {id:room.id})}}">{{room.title}}</a>
                    {% endfor %}
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Enfants
                </a>
                <div class="dropdown-menu dropM" aria-labelledby="navbarDropdown">
                    {% for child in childs %}
                        <a class="dropdown-item" href="{{path('child', {id:child.id})}}">{{child.title}}</a>
                    {% endfor %}
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Escape mobile
                </a>
                <div class="dropdown-menu dropM" aria-labelledby="navbarDropdown">
                    {% for m in mobiles %}
                        <a class="dropdown-item" href="{{path('mobile', {id:m.id})}}">{{m.title}}</a>
                    {% endfor %}
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('citygame')}}">Dans la ville</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('entreprise')}}">Entreprises</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Réserver
                </a>
                <div class="dropdown-menu dropM" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="https://bookeo.com/daedalusescapegameblois?category=4255043TPNX15F160DD994">RESERVER – Nos Salles</a>
                    <a class="dropdown-item" href="https://bookeo.com/daedalusescapegameblois?category=425506AMHEC1762E4B5380">RESERVER – Escape Game Mobile</a>
                </div>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    Autres informations
                </a>
                <div class="dropdown-menu dropM" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{path('tarif')}}">Tarifs</a>
                    <a class="dropdown-item" href="{{path('contact')}}">Contact</a>
                    <a class="dropdown-item" href="{{path('allActu')}}">Actualités</a>
                    <a class="dropdown-item" href="{{path('allAlbum')}}">Albums photos</a>
                    <a class="dropdown-item" href="{{path('faq')}}">FAQ</a>
                    <a class="dropdown-item" href="{{path('comment')}}">Livre d'or</a>
                </div>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{path('partenaires')}}">Partenaires</a>
            </li>
        </ul>
    </div>
</nav>